{
  "mcpServers": {
    "ovr_github": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "$GITHUB_PERSONAL_ACCESS_TOKEN"
      }
    },
    "ovr_slack": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-slack"
      ],
      "env": {
        "SLACK_BOT_TOKEN": "$SLACK_BOT_TOKEN"
      }
    },
    "ovr_repomix": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-repomix"
      ]
    },
    "ovr_filesystem": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-filesystem"
      ],
      "env": {
        "ALLOWED_DIRECTORIES": "/Users/bprzybysz/nc-src/ovora/manager"
      }
    }
  },
  "tools": {
    "computer_use": {
      "enabled": false
    },
    "bash": {
      "enabled": true
    },
    "edit": {
      "enabled": true
    }
  },
  "customInstructions": [
    "You are working on the Ovora Manager component, which is the core backend of an AI-powered system administration platform.",
    "Always follow the patterns established in the manager codebase located in /Users/bprzybysz/nc-src/ovora/manager/src/",
    "When implementing new features, reference existing patterns in modules/, tools/, and frameworks/graphmcp/",
    "Use the GraphMCP framework for complex workflow orchestration",
    "Follow the testing patterns established in the codebase with comprehensive unit and integration tests",
    "Maintain compatibility with the broader Ovora ecosystem (Agent and UI components)",
    "Use context engineering principles: comprehensive context, detailed examples, validation gates, consistent patterns"
  ],
  "rules": [
    "Always read CLAUDE.md first to understand manager-specific patterns",
    "Use existing database patterns from src/database/client.py",
    "Follow FastAPI patterns established in src/api.py and modules",
    "Leverage GraphMCP framework for workflow automation",
    "Maintain microservices architecture for tools in src/tools/",
    "Use Celery patterns for background task processing",
    "Follow environment configuration patterns in src/config.py",
    "Implement comprehensive error handling and logging",
    "Write tests following existing pytest patterns",
    "Update documentation when adding new patterns or features"
  ],
  "permissions": {
    "allow": [
      "WebFetch(domain:raw.githubusercontent.com)"
    ],
    "deny": []
  }
}